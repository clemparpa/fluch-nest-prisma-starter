# In dev, Vite proxies /api → http://localhost:3000 (see vite.config.ts).
# Leave VITE_API_BASE_URL blank to use the relative /api path through the proxy.
# In prod, override to the absolute API origin if front and back live on
# different domains.
# VITE_API_BASE_URL=https://api.fluch.com
